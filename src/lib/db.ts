import { db } from './firebase';
// import { collection, getDocs, query, where, addDoc } from 'firebase/firestore';

// SERVICES and Mock Booking Logic removed as part of Cal.com migration.
// If needed, we can re-add specific Firebase helpers here.

export const dbInstance = db; 

